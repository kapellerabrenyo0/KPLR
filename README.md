# KPLR

Landing page for KPLR — spatially honest AI-generated real estate property walkthrough videos.

Plain HTML/CSS/JS, no build step. Deploy as a static site (e.g. Vercel — just import the repo, no config needed).

## Local preview

Open `index.html` directly in a browser, or serve the directory with any static file server:

```
npx serve .
```
